#include "main.h"

/**
 * _abs - compute absolute value of an integer
 *
 * @n: number to compute the absolute value
 *
 * Return: Always 0 as success
 */
int _abs(int n)
{
if (n >= 0)
	return (n);
else
	return (n * -1);
}
